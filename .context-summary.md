# EMS Scout Context

更新时间：2026-07-18 +08:00

## 当前结论

- C#/.NET 10 负责 WinUI、契约、SQLite、质量、对账、Excel、错误和日志。
- Node 仅负责 Playwright/Edge CDP 采集与 Sidecar 协议适配。
- PowerShell 仅负责 Windows 打包和隔离现场 E2E。
- 唯一用户导出：`数据管理 -> 导出当前筛选 Excel`。
- 品牌显示统一为 `EMS Scout`，C# 程序集和命名空间统一为 `EmsScout`。
- 跨平台重构计划完成 21/21；重构已按四组提交。
- Windows CI 工作流已扩展为 XAML、干净克隆测试、Sidecar payload smoke、测试签名 MSIX 和干净用户安装生命周期，并由 Run #14（`29636547447`）完成远端闭环。
- 2026-07-13 原生 UI P0-A/P0-B/P0-C/P0-D 已完成本机门禁：Debug AppX 可自包含启动，主导航收敛为 5 个工作流入口和 2 个底部工具入口，工作台使用持久化待处理队列，设备数据具备问题快捷筛选、详情和一致导出。
- 2026-07-14 已退役日期管理月历、深链接和专属计划审计表面；区域管理保留关注起止日期选择器，SQLite 日期计划历史与 API 保持不变。
- 区域管理三轮独立审计未发现 P0，但确认多项 P1/P2 体验与功能问题；本轮仅记录问题，不把审计发现误报为已修复。
- 2026-07-15 已修复区域管理 P1/P2：关注规则加载会话化、刷新保留草稿、设备 UID/card 精确身份、系统组成员统计、楼层停用持久化、关注异常精确跳转、窄窗关注排期和 CJK 筛选摘要断字。
- 2026-07-16 已按方案一完成区域组重构：删除用户可见“关注设备”，现有设备目录支持楼层持续规则、精确设备名、设备名关键字和人工成员；正式成员变更统一进入审计确认。
- schema v6 新增区域组规则、正式成员、长期屏蔽/手动保留例外和待确认加入/移除；公区、非公区为可改名、停用、删除且不会重建的一次性预设。
- 2026-07-18 完成区域组方案一最后阻断：V006 mixed UID/noUID 不扩张、四类成员保护字段回滚、目录超限 fail-closed、noUID 同名统计、UID occurrence/大小写统一和旧 schema `source_key` 兼容均有回归覆盖。
- 测试签名 Release MSIX 已在干净云端 runner 完成两轮安装、AUMID 启动、卸载及包/证书清理；正式生产签名、跨版本升级、内置 Sidecar 实采和真实 EMS 尚未验证，不得声明最终交付完成。

## 当前工作树

- 重构主体文档提交：`5d59c52 docs: consolidate architecture and migration status`。
- 四组重构提交：`b362d30`、`f0b8fcb`、`aeb6739`、`5d59c52`。
- 提交后的状态同步另行记录，不在本文内固定自身提交哈希。
- 当前工作树包含本轮日期管理退役、区域管理修复、三轮审计记录及用户既有未提交工作；不得据此批量暂存或清理。
- 不碰 `data/1号楼`、`data/2号楼` 未跟踪 WAL/SHM。
- 不通过 SQLite 打开生产 `out/ac.db`；测试使用字节复制到临时目录。

## 已完成能力

- CollectionSnapshot、WorkflowEvent、WorkflowControl v1。
- C# v0/v1/v2 schema migration、schema guard、稳定身份和 alias/ambiguity ledger。
- 单栋/全量事务导入、幂等、取消和故障回滚。
- Node/C# 共享质量 fixture 与 run17 黄金清单。
- Excel 内容级契约和 ExportSmoke。
- `CollectionTaskViewModel` 拆出环境探测、进度解析和对账行模型。
- 9 类错误、稳定错误码、安全 UI 文案。
- 原生 NDJSON 日志、脱敏、截断、并发安全、诊断页发现。
- 生产 DB 测试统一使用 `ProductionDataSnapshot`。
- 待处理队列使用稳定问题 ID、`未处理/已确认/已忽略/已解决` 状态、忽略原因和状态历史；成功读取后问题消失可自动解决，失败来源不会误解决。
- SQLite schema v5 新增 `attention_issues` / `attention_issue_history`，不修改原始采集或审计证据。
- 设备数据提供离线、未知、温度异常、无实时数据、需关注 5 个带数量快捷筛选；筛选矩阵默认折叠，结果表与 360px 详情栏在首屏并列。
- 设备查询使用版本化不可变快照：旧响应不能覆盖新选择，读取失败保留上次成功结果；筛选变化后必须重新查询才能导出。
- Excel 预览数量、最后成功查询和实际工作簿行数共用同一查询口径；服务在写文件前拒绝 `Total` 与加载行数不一致。
- 采集页运行前检查固定展示本地组件、当前数据、采集浏览器和 EMS 页面四项状态，以进度、颜色图标和轻量刷新动画直接呈现通过与阻塞。

## 当前验证

- 方案一精确验收：113/113。
- .NET 非生产证据（`Fixture!=ProductionEvidence`）：339/339。
- 日期退休、区域设置、关注规则、设备身份、楼层停用和导航聚焦契约均纳入 native:test。
- Node：122 项，120 pass、2 个缺生产 fixture 的明确 skip。
- `npm run self-test`：通过。
- Windows 原生解决方案与桌面运行 build（`EnableMsixTooling=false`）：0 warning / 0 error。
- 当前 Debug 桌面运行输出：0 warning / 0 error；隔离临时数据库重新完成区域组/审计真实 WinUI QA。
- 本机 Release MSIX 为 120,627,102 bytes，SHA-256 `AA930B330BDCD44D0B215785573D7688EAE945996B5029C7003864349C352DD9`，签名状态 Valid；当前开发机存在 Debug development-mode 包，因此安装脚本只验证了 fail-closed，完整生命周期由干净云端 runner 验收。
- GitHub Actions Run #14（`29636547447`，提交 `d41b25d`）：8m04s 成功；签名 MSIX 构建、两轮安装/AUMID 启动/卸载、包清理、CurrentUser/LocalMachine 证书清理和 Artifact 上传全部通过。制品 `ems-scout-windows-x64-14` 为 237 MB，摘要 `sha256:e1bbf56fd3feb10754a538310947ef0b1b1b5b979ae26eba6600be233cc395d2`。
- 隔离运行态命令：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-native.ps1 -NoBuild -UiValidation`。
- 隔离运行态已验证“区域组”与“审计”方案一链路；用户可见的关注设备、关注时间窗和关注事件入口均不存在。验证使用唯一 `%TEMP%\ems-scout-ulw` 合成数据库，未接触仓库 `data/` 或 `out/` 数据库。
- 全新空数据目录会自动创建 schema v6 空库；此前工作台待处理队列验收仍作为历史证据保留。
- 设备数据在 949x540 压力窗口和 1280x768 最大化窗口完成空态、折叠/展开、快捷筛选、详情、历史只读与导出确认验收；未发现文字或操作重叠。
- 采集页在 1280x752 隔离窗口验证四项检查始终可见，`2/4 已通过` 进度与低饱和状态图标一致，状态文字和圆底保持主题灰色；未启动真实采集。
- 合成临时库快捷计数为离线 1、未知 1、温度异常 2、无实时数据 5、需关注 4；导出确认 5 台，生成工作簿 `devices` 为 5 行、12 列。
- P0-D 历史临时 DB schema 审计：v5、`quick_check=ok`、current、pending=0；该记录早于本轮 schema v6。
- `dotnet format`：成功，仅 Desktop 未加载警告。
- ExportSmoke 临时 DB：`2.5F` 7 行；`xlsx` 验证 `devices`、12 列、`A1:L8`。
- GitHub Actions `29148522662`：XAML/MSIX/Sidecar/迁移/漏洞审计全部通过。

## run17 黄金数据

- 6 栋、143 子区、373 页。
- 6571 raw、6568 unique。
- 开机 1843、关机 3196、离线 1527、未知 2。
- quality_pass 356、offline_template_stable 5、device_anomalies_preserved 11、known_source_indicator_missing 1。
- 原生质量：3 个阻断问题、7 条非阻断已知发现。

## 生产哈希

```text
enum_full_v5.json  80c5f175b043387beb36fb64e4b0b573c919141ed9a32ef416af174e1d205716
quality_report     3c164fa0d0c9aaf3ffbb9e8eb55a4b20c9ee686eeb3543f5879ddcb3641ea2f3
ac.db              7bf26085ac5d8d0259c29bdcfdaaa72ce6cc4f31bacb478e20f5c72118350cda
ac.db-wal          e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
ac.db-shm          fd4c9fda9cd3f9ae7c962b0ddf37232294d55580e1aa165aa06129b8549389eb
```

## Git 审计

- 四组历史重构提交已完成；当前工作分支为 `codex/collection-recapture-ui`。
- 已提交删除项的真实运行时引用已清空。
- `git diff --check HEAD` 通过；生产 `out/*` 哈希未变化。
- 4 个 `data/*/ac.db-wal|shm` 是受保护证据，必须排除。

## 2026-07-13 UI 安全修正

- 首次运行态预检只检查了未打包的 `%LOCALAPPDATA%`，未覆盖 MSIX 虚拟化配置目录；该次界面读取了配置指向的仓库数据目录，但未触发保存、导出、采集或编辑操作，因此不作为隔离验收证据。
- 最终有效 UI 证据全部使用 `-UiValidation`，配置、数据和导出目录位于唯一 `%TEMP%\ems-scout-ui-validation-*` 目录。
- 验证前后打包用户设置 SHA-256 均为 `E028D0D08F37EDBE80AE8CF497F7CEBFBF127D0B00094FF64464C22892746729`。

## 2026-07-14 区域管理三轮审计

- P1：关注规则异步读取完成前可以保存默认值并覆盖旧规则；切组、刷新或新建会静默丢弃分组/成员草稿。
- P1：系统组允许添加人工成员但统计不使用这些成员；设备成员未保留 v2 UID/page/occurrence，重名设备会折叠并扩大关注范围。
- P1/P2：发现楼层“停用”后同步立即恢复启用；关注异常跳转忽略子区；加载存在未版本化并发覆盖、无用全量查询和大历史内存过滤。
- P2：升级库可能保留指向已退役日期管理的旧系统说明；导航仍名为“规则与计划”；窄窗固定双栏、错误状态、焦点交接和可访问说明需要改进。
- 审计证据为源码路径、隔离 WinUI 运行态和临时 fixture；未打开生产数据库，未执行 ProductionEvidence。

## 2026-07-15 区域管理修复

- 已修复关注规则异步覆盖：编辑会话成为唯一加载权威，旧响应不能污染新选择，保存/删除要求当前 ready rule。
- 已修复刷新草稿丢失：刷新期间禁止破坏性切组，新建/刷新失败保留编辑器、成员草稿和设备 UID。
- 已修复设备身份与系统组统计：成员使用稳定 UID/card id，系统分类与人工成员合并统计并按 UID 去重。
- 已修复楼层停用回弹：发现目录同步保留用户显式停用状态；UI 语义从“删除楼层”收敛为“停用楼层”。
- 已修复关注异常定位过宽：跳转请求带楼栋、楼层、子区、页面、设备 UID/card id，SQLite 查询可按 card id 精确过滤。
- 已修复窄窗体验：关注排期单列显示，关注事件按钮保持可见；数据筛选摘要三行换行并保护短中文标签不断字。
- 真实 WinUI 隔离链路已复验：区域组与关注 -> 夜间重点区 -> 查看异常 -> 设备数据，命中 1 条 `COLLISION-KT`，筛选摘要精确到 `1号 / 1F / 1F A / default / COLLISION-KT`；CJK 复审确认“子区”未拆字。

## 下一步

1. 使用正式生产证书完成跨版本 MSIX 升级验收，并验证内置 Sidecar 实际采集。
2. 在已登录真实 EMS 环境执行单栋及六栋现场验收。
3. 后续可继续做区域页键盘/Narrator 深验和大库对账压力测试。
4. 外部门全部通过后，再评估 protected legacy 删除和发布。

## 关键文档

- `docs/architecture.md`
- `docs/项目规范.md`
- `docs/Windows验证清单.md`
- `docs/状态.md`
- `docs/交接.md`
- `docs/legacy-inventory.md`
- `native/docs/MIGRATION_PLAN.md`
- `tests/fixtures/run17/golden-v1.json`
