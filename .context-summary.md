# EMS Tool Audit Fix Context

更新时间：2026-07-10 15:05 +08:00

## 2026-07-10 最近采集失败核查与最终全量采集

- 最近失败并非漏楼/漏页/漏卡：两次拒绝结果均有 6 栋、6568 张唯一卡；生产 JSON/DB 在质量门失败时未覆盖。
- 根因分两层：
  - 最终审计未承认重试阶段已接受的稳定离线原因，形成前后规则矛盾。
  - 11 页各有 1 台持续 EMS 设备字段异常，旧严格页级门槛把源数据异常当成整页采集失败。
- 已修正：
  - 最终审计统一采集质量原因。
  - `device_anomalies_preserved` 只接受连续 3 次稳定、结构/通讯/indicator/开关完整、异常不超过 2 台且不超过页面 10% 的页面。
  - 删除会接受未知通讯的 `stable_partial`。
  - 2号 2.5F 两台 `2-2BC-2M001-KT-1/-2` 为精确已知 EMS 无 indicator 缺陷；输出保持 `comm` 未知，不使用邻卡 indicator 推断。
- 最终生产结果：
  - JSON：`out/enum_full_v5.json`，completedAt `2026-07-10T06:52:48.242Z`。
  - DB 最新批次：run 17，completed/full，6568 张。
  - 楼栋：1号 1493、2号 107、3号 1106、4号 1096、5号 286、6号 2480。
  - 状态：开机 1843、关机 3196、离线 1527、未知 2。
  - 页面原因：quality_pass 356、offline_template_stable 5、device_anomalies_preserved 11、known_source_indicator_missing 1；无空原因、无 `stable_partial`。
  - 导入前备份：`out/ac_before_full_import_20260710_144208.db`。
- 质量报告：`out/quality_report_run17.json/.txt`，issue_count=3，仍未全绿：
  - P2：4 个未登记的稳定全离线模板页待人工确认。
  - P1：10 台持续异常/缺字段设备。
  - P1：上述异常影响的 10 个活动设备字段不完整页面汇总。
  - 两台 2号无 indicator 设备和 1号已核实源缺陷已登记为非阻断 known findings，但保持可见。
- 状态统计已改为 comm 优先且唯一：`switch=OFF` 不再把未知通讯误计为关机。
- 验证：
  - `npm run self-test` 通过。
  - 原生测试 107/107 通过。
  - 数据管理 Excel 烟测通过，输出 `out/export-smoke-run17/数据管理筛选结果_20260710_150314.xlsx`；单一 devices 工作表、12 列、6570 行（含 2 个虚拟纳管设备）。
  - 未运行 legacy TXT/Markdown/多格式报表。

## 2026-07-07 09:05 现场 E2E `-LaunchEdge` 加固

- `scripts/field-e2e.ps1` 新增并加固 `-LaunchEdge`：
  - 自动打开隔离 Edge，不杀用户已有 Edge。
  - 使用运行期随机本机 CDP 端口，避免误连已有 9222 会话。
  - 显式绑定 `--remote-debugging-address=127.0.0.1`。
  - 使用本次 `out/field-e2e-*\.edge_profile` 独立 Profile。
  - 默认在结束或失败时只关闭本次 Profile 对应 Edge 进程并删除 Profile。
  - 如需保留浏览器或登录态，显式使用 `-KeepBrowser` / `-KeepProfile`。
- `src/enumerate.js` 新增 `--fail-if-not-logged-in`：
  - 现场 verify/采集未登录时退出码 3 快速失败。
  - 避免非交互脚本进入 stdin 帮助菜单卡住。
- `field-e2e.ps1` 的 EMS 页识别改为按 `$EmsUrl` host/path 判断，不再硬编码 `172.29.248.4`。
- 新增只读对抗审查 Agent D，发现并已处理：
  - 避免误连已有 CDP。
  - CDP 只允许 loopback。
  - 默认清理 profile 中的 EMS 登录态。
  - 登录判断只能作预检，最终仍跑 `--verify`。
- 当前实测：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\field-e2e.ps1 -Building 1号 -LaunchEdge -LoginWaitSeconds 5`
  - 启动隔离 Edge，随机端口 `56518`；EMS HTTP 200；未登录时 `enumerate.js --fail-if-not-logged-in` 退出码 3；本次 Edge 和 profile 已清理。
- 已验证：
  - `node --check src\enumerate.js` 通过。
  - `dotnet build native\EmsScout.Native.slnx -c Debug --no-restore /p:UseSharedCompilation=false` 通过。
  - `npm run self-test` 通过。
  - `dotnet test native\tests\EmsScout.Tests\EmsScout.Tests.csproj -c Debug --no-restore /p:UseSharedCompilation=false`：60/60 通过。
- 仍未完成：
  - 真实 EMS 单栋采集未验证。
  - 下一步现场命令：
    `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/field-e2e.ps1 -Building 1号 -LaunchEdge -RunSingleBuilding`
  - 需要在脚本打开的隔离 Edge 中完成 EMS 登录。

## 2026-07-07 08:50 现场 E2E 安全工具与预检防误导

- 新增 `scripts/field-e2e.ps1`，作为真实 EMS 现场端到端验证唯一安全入口：
  - 默认只检查 EMS HTTP、Edge CDP、EMS 标签页/登录验证，不采集、不导入。
  - 加 `-RunSingleBuilding` 后执行单栋链路：采集 → 临时 JSON → 临时 SQLite → 质量报告 → 数据管理筛选 Excel 烟测。
  - 输出统一写入 `out/field-e2e-YYYYMMDD_HHMMSS/`。
  - 内置绝对路径防护，拒绝把临时 `EMS_DB_PATH` 指到生产 `out/ac.db`。
- 新增 `native/tools/EmsScout.ExportSmoke/`：
  - 复用原生 `SqliteDeviceExportService` 生成 `数据管理筛选结果_yyyyMMdd_HHmmss.xlsx`。
  - 验证 workbook ZIP 结构和 `summary/devices/filters` 三张表。
  - 默认不传 `SqliteDeviceWatchRepository`，避免烟测导出时对 DB 建 watch 表。
- 原生采集预检已强化：
  - `CollectionTaskViewModel` 增加 Node 依赖检查。
  - Edge CDP 检查从 `/json/version` 扩展到 `/json/list`，查找 EMS 标签页。
  - EMS 登录态显示为“未验证”，不再把 CDP 可达暗示为已登录可采集。
  - 启动枚举前若启用登录态检查且未发现 EMS 标签页，直接阻止启动，避免 WinUI 进入无 stdin 的 Node 登录等待。
  - `TasksPage` 文案改为“打开 EMS 页面”“运行前检查”。
- 已运行验证：
  - `dotnet restore native\EmsScout.Native.slnx` 通过。
  - `dotnet build native\tools\EmsScout.ExportSmoke\EmsScout.ExportSmoke.csproj -c Debug --no-restore /p:UseSharedCompilation=false` 通过。
  - `dotnet build native\EmsScout.Native.slnx -c Debug --no-restore /p:UseSharedCompilation=false` 通过。
  - `dotnet test native\tests\EmsScout.Tests\EmsScout.Tests.csproj -c Debug --no-restore /p:UseSharedCompilation=false`：60/60 通过。
  - `npm run self-test` 通过。
  - `node scripts\validate-enum.js` 通过。
  - `dotnet format native\EmsScout.Native.slnx --verify-no-changes --no-restore` 通过，仍有既有工作区加载警告。
  - ExportSmoke 对 `out/ac.db` 的 `1号 + 公区` 筛选导出通过，生成 201 行 Excel。
  - `scripts\field-e2e.ps1 -Building 1号` 当前安全失败：EMS HTTP 200；CDP `127.0.0.1:9222` 连接被拒绝；未进入采集/导入。
  - 离线后半链路通过：现有 JSON → 临时 DB → 临时质量报告 → 临时 Excel，输出 `out/field-e2e-offline-20260707_084559/`。
- 仍未完成：
  - 真实 EMS 单栋采集未验证。
  - 需要用户在可通过 `127.0.0.1:9222` 访问的 Edge/CDP 会话中登录 EMS 后运行：
    `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/field-e2e.ps1 -Building 1号 -RunSingleBuilding`

## 2026-07-07 真实 EMS 端到端稳定性审查

- 用户目标：测试真实 EMS 环境里的端到端稳定性，并开启多个 Agent 做对抗性审查。
- 已开启 3 个只读对抗性审查 Agent：
  - 采集 -> JSON/SQLite -> 原生数据管理 -> Excel 导出链路。
  - 原生 UI/唯一 Excel 导出/诊断页。
  - legacy 报表、旧 Web、旧批处理和历史输出物。
- 已修复会误导进度的关键问题：
  - `src/tui/actions.js`：正确处理 `[ACTION]switch_to_cdp` / `[ACTION]return`，避免返回/切换后继续导入旧 JSON。
  - `scripts/import.js`：清库、插入、更新时间戳、同步楼层目录、创建历史批次改为单个 SQLite transaction。
  - `src/enumerate.js`：CDP 模式也检查 EMS 登录状态。
  - `src/rules.js`：当前卡数基线修正为 2号 107、5号 286、6号 2480。
  - `DataViewModel.ExportAsync()`：导出前同步当前筛选，避免屏幕结果和 Excel 条件不一致。
  - 最近 Excel 列表使用严格 `数据管理筛选结果_yyyyMMdd_HHmmss.xlsx` 文件名。
  - 诊断页日志读取支持共享读取，运行中日志也可预览。
- 已收口 legacy 误导入口：
  - `scripts/report.js` / `dump-aircons.js` / `dump-public.js` 默认禁用；应急运行需 `EMS_ENABLE_LEGACY_REPORTS=1`。
  - `package.json` 中旧 Web/Electron 入口改为 `legacy:*`。
  - `enum:edge` 改为干净 CDP 全量枚举，追加模式为 `enum:edge:append`。
  - Electron legacy 名称改为 `EMS Legacy Web Panel`。
  - 旧报表输出归档到 `out/legacy-report-archive/`。
  - 旧 Electron 发行物归档到 `out/legacy-electron-dist-archive/dist_20260707_082001/`，当前 `dist` 为空。
- 真实 EMS 探测证据：
  - `http://172.29.248.4:8000/ui` HTTP 200。
  - `http://127.0.0.1:9222/json/version` 连接被拒绝。
  - `node src\enumerate.js --auto-launch --verify --log-level=DEBUG --log-category=ENUM,QUALITY,CRASH --log-file` 写入 `out/verify-probe/enum_2026-07-07.log`，结果为自动启动 Edge 后 60 秒未检测到 EMS 登录状态。
  - 已清理本次自动启动的临时 Edge 进程。
  - 不能标记真实 EMS 端到端稳定性完成；需要已登录 EMS 会话或用户现场完成登录后继续跑 `--verify` / 单栋采集。
- 已验证：
  - `node --check` 覆盖 TUI、导入、枚举、规则、legacy 报表和 Electron legacy 入口。
  - `dotnet build native\EmsScout.Native.slnx -c Debug --no-restore /p:UseSharedCompilation=false` 通过。
  - `dotnet test native\tests\EmsScout.Tests\EmsScout.Tests.csproj -c Debug --no-restore /p:UseSharedCompilation=false`：60/60 通过。
  - `npm run self-test` 通过。
  - `node scripts\validate-enum.js` 通过。
  - 临时库 `out/e2e_tmp_ac.db` 导入烟测通过，合计 6568 卡。
  - `dotnet format native\EmsScout.Native.slnx --verify-no-changes --no-restore` 通过，仍有既有工作区加载警告。
  - `node scripts\quality-report.js` 通过，剩余 2 个 P2 数据质量提示：未知通讯 3、非离线卡缺少 indicator 原图 3。
  - 扫描确认默认入口不再包含 `npm run panel`、`npm run desktop`、旧 Electron 产品名字样、旧对账导出接口字样。

## 2026-07-06 当前原生程序全面可用性检查与旧面板对比

- 本轮对当前 WinUI 原生程序做了端到端可用性检查，并与旧 Web 面板入口做对照。
- 已启动原生程序，UI Automation 逐页导航通过 7 个顶级页面：
  - 总览：193 行 UIA 快照，含优先处理、质量/实时/对账风险、当前口径、状态分布、楼栋状态。
  - 采集任务：265 行 UIA 快照，含开始/停止、检查环境、打开 EMS、采集范围、高级参数、质量审计、实时审计、实时对账、历史批次。
  - 数据管理：762 行 UIA 快照，含数据口径、区域组、快捷筛选、高级筛选、分页、最近 Excel、导出当前筛选 Excel、设备列表和详情。
  - 审计中心：328 行 UIA 快照，含基础质量审计、实时点位审计、实时对账、历史批次恢复/删除。
  - 分组设置：215 行 UIA 快照，含区域组、楼层目录、成员维护、关注设备、关注事件。
  - 系统设置：48 行 UIA 快照，含连接/目录/导出目录、默认采集模式、最近 Excel 跟踪。
  - 诊断：231 行 UIA 快照，含应用信息、路径检查、流程口径、日志查看、最近 Excel。
- 新增/确认关注事件集中视图：
  - 新增 `native/src/EmsScout.Desktop/ViewModels/WatchIncidentRow.cs`。
  - `GroupsViewModel` 暴露 `WatchIncidents`、`SelectedWatchIncident`、`WatchIncidentSummary` 和 `OpenSelectedWatchIncident()`。
  - `AreasPage.xaml` 在关注设备区域新增“关注事件”列表和“查看异常”按钮。
  - `AreasPage.xaml.cs` 新增 `OpenWatchIncident_Click`。
  - 点击“查看异常”跳转数据管理，带入设备名、楼栋、区域组和 `WatchState=abnormal`。
- 旧 Web 面板功能对比：
  - 已覆盖并进入原生主流程：总览、采集任务、数据管理筛选/分页/详情、备注标签、实时匹配覆盖、分组设置、楼层目录、关注时间窗、关注异常筛选/导出证据、质量审计、实时审计、实时对账、历史批次恢复/删除、诊断日志、最近 Excel。
  - 暂未原样恢复但已有替代：旧 Web `monitored_floors` / `floor_monitor_snapshots` / `floor_monitor_events` 独立楼层监控流；建议继续用“分组设置 -> 关注设备 -> 关注事件”吸收，不恢复旧表驱动独立入口。
  - 暂未原样恢复但不建议做全局主入口：旧 Web 顶部全局批次下拉；原生已在数据管理提供历史批次只读预览，并在审计中心提供恢复当前数据。
  - 不恢复到原生主流程：`scripts/report.js` 一键多格式报表、`scripts/dump-aircons.js`、`scripts/dump-public.js`、TXT / Markdown 导出、旧 Web 报表列表和文件下载入口。
- 验证：
  - `dotnet build native\EmsScout.Native.slnx -c Debug --no-restore /p:UseSharedCompilation=false`：0 警告，0 错误。
  - `dotnet test native\tests\EmsScout.Tests\EmsScout.Tests.csproj -c Debug --no-restore /p:UseSharedCompilation=false`：60/60 通过。
  - `npm run self-test` 通过。
  - `dotnet format native\EmsScout.Native.slnx --verify-no-changes --no-restore` 通过，仍有既有工作区加载警告。
  - `PrintWindow` 输出 `out/native-usability-groups-printwindow.png`；截图捕获时分组列表仍处于加载态，最终可用性以加载完成后的 UIA 文本快照为准。
  - 扫描和 UIA 均确认原生 UI 仍只有 `数据管理 -> 导出当前筛选 Excel` 作为用户导出路径。

## 2026-07-06 数据管理可用性整改与旧面板对比

- 本轮继续按“重构而非迁移”推进，重点是证明数据管理页可用，而不是恢复旧面板所有入口。
- 联网核对 Microsoft Learn WinUI 控件方向：
  - NavigationView 用于多页顶层导航。
  - InfoBar 用于内联状态提示，不打断工作流。
  - ComboBox 用于数据口径和筛选项单选。
  - ListView/GridView 用于数据集合和指标集合。
- 修复数据管理页布局：
  - 快捷统计 `GridView` 与主列表原本同在 `Grid.Row=3`，会视觉叠压；主列表/详情移到独立内容区。
  - 主筛选从超宽固定列改成两行：数据口径/楼栋/通讯/区域/搜索 + 区域组/快捷筛选/结果摘要/分页。
  - 高级筛选拆成三行等宽列，降低横向挤压。
  - 分页按钮加无障碍名称和 Tooltip。
  - `DataSource_SelectionChanged` 增加初始化保护。
- 修改文件：
  - `native/src/EmsScout.Desktop/Pages/DataPage.xaml`
  - `native/src/EmsScout.Desktop/Pages/DataPage.xaml.cs`
  - `native/src/EmsScout.Desktop/ViewModels/DataViewModel.cs`
  - `CHANGELOG.md`
  - `.context-summary.md`
- 当前原生功能对比结论：
  - 已覆盖主流程页：总览、采集任务、数据管理、审计中心、分组设置、系统设置、诊断。
  - 分组设置已支持保存/停用楼层目录、添加/删除楼层/子区/设备成员、保存/删除关注时间窗。
  - 关注设备已支持关注窗口内 ON/OFF 变化异常标记，数据管理可筛选并导出关注证据。
  - 不建议恢复：`scripts/report.js` 多格式报表、`dump-aircons.js`、`dump-public.js`、旧文件下载/报表列表入口。
  - 可后续吸收：旧 Web 楼层监控事件流，建议并入分组设置/关注设备事件视图。
- 已验证：
  - `dotnet build native\EmsScout.Native.slnx -c Debug --no-restore /p:UseSharedCompilation=false`：0 警告，0 错误。
  - `dotnet test native\tests\EmsScout.Tests\EmsScout.Tests.csproj -c Debug --no-restore /p:UseSharedCompilation=false`：60/60 通过。
  - `npm run self-test` 通过。
  - `dotnet format native\EmsScout.Native.slnx --verify-no-changes --no-restore` 通过，仍有既有工作区加载警告。
  - 运行态 UI Automation 找到数据管理关键控件：数据口径、区域组、快捷筛选、高级筛选、导出当前筛选 Excel、上一页、下一页。
  - `PrintWindow` 截图确认主列表不再被快捷统计条遮挡：`out/native-data-page-window.png`。
  - 原生 UI 扫描仍只有 `数据管理 -> 导出当前筛选 Excel`，未恢复 TXT / Markdown / 一键三报表。

## 2026-07-06 数据管理历史批次只读预览

- 本轮继续按“重构而非迁移”推进数据管理可用性，补齐历史批次查看能力，避免用户为了查看历史而恢复当前 DB。
- 联网核实官方 Windows/WinUI 方向：
  - 单一数据口径切换适合用 ComboBox。
  - 结果列表和详情继续沿用 ListView/详情面板。
  - 历史只读状态用 InfoBar 明确提示，避免误操作。
- 新增数据层能力：
  - `DeviceQuery.RunId`。
  - `SqliteDeviceReadRepository` 根据 RunId 切换当前表或历史 `run_*` 表。
  - `LoadFilterOptionsAsync(DeviceQuery)` 支持历史口径筛选项。
  - 历史口径不附加实时详情、关注窗口、备注/标签/覆盖，保持只读历史事实。
- 数据管理 UI：
  - 筛选区新增 `数据口径` 下拉：`当前数据` / `历史 #ID`。
  - 历史口径显示 `历史批次只读预览` InfoBar。
  - 历史口径下导出、备注、标签、实时覆盖、忽略重复均禁用或阻止。
  - 当前数据仍是唯一可导出的业务口径。
- 修改文件：
  - `native/src/EmsScout.Application/Devices/DeviceQuery.cs`
  - `native/src/EmsScout.Application/Devices/IDeviceReadRepository.cs`
  - `native/src/EmsScout.Infrastructure/Sqlite/SqliteDeviceReadRepository.cs`
  - `native/src/EmsScout.Desktop/ViewModels/DataSourceOption.cs`
  - `native/src/EmsScout.Desktop/ViewModels/DataViewModel.cs`
  - `native/src/EmsScout.Desktop/Pages/DataPage.xaml`
  - `native/src/EmsScout.Desktop/Pages/DataPage.xaml.cs`
  - `native/tests/EmsScout.Tests/SqliteDeviceReadRepositoryTests.cs`
- 已验证：
  - 新增 `SearchesHistoricalRunSnapshotWithoutRestoringCurrentDatabase`。
  - `dotnet build native\EmsScout.Native.slnx -c Debug --no-restore /p:UseSharedCompilation=false`：0 警告，0 错误。
  - `dotnet test native\tests\EmsScout.Tests\EmsScout.Tests.csproj -c Debug --no-restore /p:UseSharedCompilation=false`：60/60 通过。
  - `npm run self-test` 通过。
  - `dotnet format native\EmsScout.Native.slnx --verify-no-changes --no-restore` 通过，仍有既有工作区加载警告。
  - 扫描确认原生 UI 未恢复 TXT / Markdown / 一键三报表。

## 2026-07-06 原生诊断页与日志查看

- 本轮按用户目标继续推进原生重构，不恢复旧多格式报表。
- 联网核实官方 Windows/WinUI 方向：
  - 顶级入口继续放在 NavigationView。
  - 非阻塞但高可见状态说明使用 InfoBar。
  - 日志/诊断类内容用列表 + 详情预览承载。
- 新增 `诊断` 顶级页：
  - 应用信息：应用名、版本、runtime、进程。
  - 路径检查：工作区、数据目录、SQLite、枚举 JSON、质量报告、导出目录、设置文件。
  - 流程口径：明确主流程为 `采集任务 -> 数据管理 -> 导出当前筛选 Excel`。
  - 日志查看：最近 `enum_*.log`、`panel_task_*.log`、`out/native-*.log`、`logs/*.log`，可预览末尾 160 行并定位文件。
  - 最近 Excel：仅展示 `数据管理筛选结果_*.xlsx`，不提供报表生成。
- 修改文件：
  - `native/src/EmsScout.Desktop/Pages/DiagnosticsPage.xaml`
  - `native/src/EmsScout.Desktop/Pages/DiagnosticsPage.xaml.cs`
  - `native/src/EmsScout.Desktop/ViewModels/DiagnosticsViewModel.cs`
  - `native/src/EmsScout.Desktop/ViewModels/DiagnosticFileRow.cs`
  - `native/src/EmsScout.Desktop/ViewModels/DiagnosticInfoRow.cs`
  - `native/src/EmsScout.Desktop/MainWindow.xaml`
  - `native/src/EmsScout.Desktop/MainWindow.xaml.cs`
  - `native/src/EmsScout.Desktop/App.xaml.cs`
  - `README.md`
  - `native/README.md`
- 已验证：
  - `dotnet build native\EmsScout.Native.slnx -c Debug --no-restore /p:UseSharedCompilation=false`：0 警告，0 错误。
  - `dotnet test native\tests\EmsScout.Tests\EmsScout.Tests.csproj -c Debug --no-restore /p:UseSharedCompilation=false`：59/59 通过。
  - `npm run self-test` 通过。

## 2026-07-06 原生 UI 可用性复查与收口

- 本轮先对当前原生应用做可用性全面检查，并与旧 Web 面板功能入口对照。
- 联网核实官方 Windows/WinUI 方向：
  - 顶级页面继续使用 NavigationView。
  - 高可见但不打断流程的状态提示适合用 InfoBar/提示条。
  - 长任务和加载过程需要明确状态/进度反馈。
- 运行检查结果：
  - 原生 6 个主页面均可导航：总览、采集任务、数据管理、审计中心、分组设置、系统设置。
  - 数据管理已读取当前筛选结果 6,570 台设备，唯一主导出仍为“导出当前筛选 Excel”。
  - 分组设置可见保存楼层、停用楼层、添加成员、删除成员、保存关注、删除关注。
  - 审计中心可见基础质量审计、实时点位审计、实时对账和历史批次。
- 本轮修正：
  - `native/src/EmsScout.Desktop/Pages/TasksPage.xaml`
    - 采集任务页增加固定任务模式说明，解释灰色 Toggle 为模式托管，不是不可用故障。
  - `native/src/EmsScout.Desktop/ViewModels/GroupsViewModel.cs`
    - 新增 `LoadingStateVisibility` / `GroupListEmptyVisibility`。
  - `native/src/EmsScout.Desktop/Pages/AreasPage.xaml`
    - 区域组列表区加入 ProgressRing 加载态和空态提示。
  - `native/src/EmsScout.Desktop/Pages/AuditPage.xaml`
    - 基础质量问题列表改为“级别 / 编码”显示，减少编码硬换行。
    - 审计指标卡高度从 96 调整到 112，降低首屏卡片裁切。
- 已复查截图：
  - `out/native-fix2-tasks.png`
  - `out/native-fix2-audit.png`
  - `out/native-fix2-groups.png`
- 自动化复核：
  - 采集任务新增说明文本可被 UI Automation 读取。
  - 审计中心 `unknown_comm` / `missing_indicator` 已作为编码字段读取，不再混入单列硬折行。
  - 分组页加载完成后仍可见保存楼层、添加成员、关注设备和关注窗口说明。
  - 采集任务页的任务模式/楼栋选择会在自动环境检查期间短暂禁用；检查结束后恢复可编辑。
- 已验证：
  - `dotnet build native\EmsScout.Native.slnx -c Debug --no-restore /p:UseSharedCompilation=false`：0 警告，0 错误。
  - `dotnet test native\tests\EmsScout.Tests\EmsScout.Tests.csproj -c Debug --no-restore /p:UseSharedCompilation=false`：59/59 通过。
  - `npm run self-test` 通过。
  - `dotnet format native\EmsScout.Native.slnx --verify-no-changes --no-restore` 通过，仍有既有工作区加载警告。
- 旧面板对照仍待决策/后续：
  - 原生没有独立“说明 / 关于”页。
  - 原生没有独立历史 `out/enum_*.log` 日志查看器。
  - 旧 Web 的“监控楼层事件流”未原样恢复；当前用“关注设备时间窗口”覆盖主要异常关注场景。

## 2026-07-06 首页风险层与审计中心执行入口

- 本轮继续推进原生 WinUI 重构目标，重点补齐“总览必须能提示真实项目风险”和“审计中心不能只读结果”的缺口。
- 联网核实官方 WinUI 方向：
  - `InfoBar` 适合非阻塞但高可见的状态/风险提示。
  - 页面级命令使用按钮组/命令按钮承载，长任务要有明确进度或状态文本。
- Application 层新增：
  - `DashboardRiskItem`
  - `DashboardRiskBuilder`
  - `DashboardOverview.Risks`
- 首页“优先处理”现在合并显示：
  - 当前数据无设备、未知通讯、离线设备。
  - 关注设备时间窗内 ON/OFF 变化。
  - 基础质量审计缺失、过期或存在问题。
  - 实时点位审计缺失、采集错误或设备字段异常。
  - 实时对账差异。
  - 历史异常隔离批次。
- 可定位风险支持跳转数据管理并自动套筛选：
  - 通讯未知 / 离线。
  - 需排查。
  - 缺实时 / 详情异常 / 点位异常。
  - 关注异常。
- `DataNavigationRequest` 新增 `WatchState`，修复首页/其他入口无法直接定位关注异常的问题。
- 审计中心顶部新增：
  - `运行基础审计`：执行 `scripts/quality-report.js`。
  - `运行实时审计`：执行 `scripts/audit-realtime-data.js`。
  - 执行后自动刷新审计中心。
- README 快速开始改为原生 `npm run native:run`，旧 Web 面板和旧多格式报表脚本明确为 legacy 保留工具。
- 修改文件：
  - `native/src/EmsScout.Application/DashboardOverview.cs`
  - `native/src/EmsScout.Application/DashboardOverviewService.cs`
  - `native/src/EmsScout.Application/DashboardRiskBuilder.cs`
  - `native/src/EmsScout.Desktop/ViewModels/HomeViewModel.cs`
  - `native/src/EmsScout.Desktop/Pages/HomePage.xaml`
  - `native/src/EmsScout.Desktop/Pages/HomePage.xaml.cs`
  - `native/src/EmsScout.Desktop/Services/INavigationService.cs`
  - `native/src/EmsScout.Desktop/ViewModels/DataViewModel.cs`
  - `native/src/EmsScout.Desktop/ViewModels/AuditViewModel.cs`
  - `native/src/EmsScout.Desktop/Pages/AuditPage.xaml`
  - `native/tests/EmsScout.Tests/DashboardRiskBuilderTests.cs`
  - `README.md`
  - `CHANGELOG.md`
  - `.context-summary.md`
- 已验证：
  - `dotnet build native\EmsScout.Native.slnx -c Debug --no-restore /p:UseSharedCompilation=false`：0 警告，0 错误。
  - `dotnet test native\tests\EmsScout.Tests\EmsScout.Tests.csproj -c Debug --no-restore /p:UseSharedCompilation=false`：54/54 通过。
  - `npm run self-test` 通过。
  - `dotnet format native\EmsScout.Native.slnx --verify-no-changes --no-restore` 通过，仍有既有工作区加载警告。
  - 扫描确认原生 `native/src` 未恢复 TXT / Markdown / 一键三报表出口，仍只有 `数据管理 -> 导出当前筛选 Excel`。
- 当前仍未完成的总目标事项：
  - 采集任务还没有旧面板那种显式“仅导入/仅审计/仅校验/仅采集 JSON”的任务模式选择。
  - 原生 UI 还需要运行后截图核验，确认首页风险表在实际窗口尺寸下不拥挤。

## 2026-07-06 分组设置楼层目录维护

- 本轮继续推进原生 WinUI 重构目标，重点补齐旧面板 `新增楼层目录` 能力。
- 联网核实官方 WinUI 方向：
  - 删除/停用等确认继续使用设置 `XamlRoot` 的 `ContentDialog`。
  - 楼层输入沿用项目已有 `ComboBox` / `TextBox` 表单风格。
- 应用层接口：
  - `IAreaGroupRepository.LoadFloorsAsync`
  - `IAreaGroupRepository.SaveFloorAsync`
  - `IAreaGroupRepository.DeleteFloorAsync`
  - 新增 `FloorCatalogRecord` / `FloorCatalogEdit`。
- SQLite 实现：
  - `SqliteAreaGroupRepository` 创建并维护 `floor_catalog`。
  - 自动同步当前 `sub_areas` 中发现的楼层到目录。
  - 手动楼层保存为 `manual`，采集重合时标记为 `manual+discovered`。
  - 删除采用软删除 `enabled=0`，不删除已有分组成员和设备数据。
- 原生分组设置页：
  - 新增“楼层目录”卡片。
  - 支持按楼栋查看目录、输入楼层、备注、保存楼层、停用楼层。
  - 成员维护里的楼层下拉改为读取楼层目录，手动新增后可立即用于“整层”成员。
  - 楼层目录楼栋与成员维护楼栋保持联动，避免新增楼层后仍停留在另一个楼栋上下文。
- 修改文件：
  - `native/src/EmsScout.Application/Groups/AreaGroups.cs`
  - `native/src/EmsScout.Infrastructure/Sqlite/SqliteAreaGroupRepository.cs`
  - `native/src/EmsScout.Desktop/ViewModels/GroupsViewModel.cs`
  - `native/src/EmsScout.Desktop/ViewModels/FloorCatalogRow.cs`
  - `native/src/EmsScout.Desktop/Pages/AreasPage.xaml`
  - `native/src/EmsScout.Desktop/Pages/AreasPage.xaml.cs`
  - `native/tests/EmsScout.Tests/AreaGroupRepositoryTests.cs`
- 已验证：
  - `dotnet test native\tests\EmsScout.Tests\EmsScout.Tests.csproj -c Debug --no-restore /p:UseSharedCompilation=false`：52/52 通过。
  - `dotnet build native\EmsScout.Native.slnx -c Debug --no-restore /p:UseSharedCompilation=false`：0 警告，0 错误。
  - `npm run self-test` 通过。
  - `dotnet format native\EmsScout.Native.slnx --verify-no-changes --no-restore` 通过，仍有既有工作区加载警告。
  - 搜索确认原生端仍未接回旧多格式报表出口。
- 当前仍未完成的总目标事项：
  - 首页还需要补质量/实时覆盖/批次健康的一屏提醒。
  - 采集任务还没有旧面板那种显式“仅导入/仅审计/仅校验/仅采集 JSON”的任务模式选择。

## 2026-07-06 数据管理快捷筛选与关注异常导出验证

- 本轮继续推进原生 WinUI 重构目标，重点修正“数据管理页面用户可用性不足”。
- 联网核实官方 WinUI 方向：
  - `ListView/GridView` 支持 `IsItemClickEnabled=true` 与 `ItemClick`，适合把统计项作为直接操作入口。
  - 本轮采用 `SelectionMode=None`，避免统计卡产生持久选中态。
- 数据管理页顶部 Facet 统计卡已改为可点击快捷筛选：
  - `全部`：清除结果类筛选，保留楼栋/搜索等上下文。
  - `开机` / `关机` / `离线`：设置通讯筛选。
  - `需排查` / `温度异常` / `集控锁定`：设置快捷筛选。
  - `公区` / `非公区`：设置区域筛选。
  - `实时详情` / `缺实时` / `手工覆盖` / `虚拟纳管`：设置实时匹配筛选。
  - `点位完整` / `点位异常`：设置实时点位筛选。
  - `已关注` / `关注异常`：设置关注设备筛选。
- 修改文件：
  - `native/src/EmsScout.Desktop/ViewModels/DataFacetItem.cs`
  - `native/src/EmsScout.Desktop/ViewModels/DataViewModel.cs`
  - `native/src/EmsScout.Desktop/Pages/DataPage.xaml`
  - `native/src/EmsScout.Desktop/Pages/DataPage.xaml.cs`
- 关注设备导出新增端到端测试：
  - `native/tests/EmsScout.Tests/DeviceWatchRepositoryTests.cs`
  - 新增 `WatchAbnormalRowsAreExportedWithEvidence`，验证 `WatchState=abnormal` 导出只包含关注异常设备，并写出 `watch_state`、`watch_window`、`watch_evidence`。
- 导出约束仍保持：
  - 只保留 `数据管理 -> 导出当前筛选 Excel`。
  - 原生端未恢复 TXT、Markdown、一键三报表、`dump-public`、`dump-aircons`、旧报表页。
- 已验证：
  - `dotnet test native\tests\EmsScout.Tests\EmsScout.Tests.csproj -c Debug --no-restore /p:UseSharedCompilation=false`：51/51 通过。
  - `dotnet build native\EmsScout.Native.slnx -c Debug --no-restore /p:UseSharedCompilation=false`：0 警告，0 错误。
  - `npm run self-test` 通过。
  - `dotnet format native\EmsScout.Native.slnx --verify-no-changes --no-restore` 通过，仍有既有工作区加载警告。
  - 搜索确认原生端仍未接回旧多格式报表出口。
- 当前仍未完成的总目标事项：
  - 首页还需要补质量/实时覆盖/批次健康的一屏提醒。
  - 旧面板 `新增楼层目录` 尚无原生入口。
  - 采集任务还没有旧面板那种显式“仅导入/仅审计/仅校验/仅采集 JSON”的任务模式选择。

## 2026-07-06 原生审计中心独立化

- 本轮继续推进原生 WinUI 重构目标，没有恢复旧 Electron/HTML 面板的多格式报表导出。
- 联网核实官方 WinUI 方向：
  - `NavigationView` 适合承载顶层页面导航。
  - 页面内破坏性/高风险操作用设置 `XamlRoot` 的 `ContentDialog` 确认。
- 新增顶级页面“审计中心”：
  - `native/src/EmsScout.Desktop/Pages/AuditPage.xaml`
  - `native/src/EmsScout.Desktop/Pages/AuditPage.xaml.cs`
  - `native/src/EmsScout.Desktop/ViewModels/AuditViewModel.cs`
- `MainWindow.xaml` 新增“审计中心”导航项，顺序为：总览、采集任务、数据管理、审计中心、分组设置、系统设置。
- `MainWindow.xaml.cs` 新增 `audit -> AuditPage` 路由。
- `App.xaml.cs` 注册 `AuditViewModel`。
- 审计中心职责：
  - 基础质量审计摘要与问题列表。
  - 实时点位审计摘要、异常分类、楼栋明细。
  - 实时源对账筛选、差异列表、规则证据、跳转数据管理定位。
  - 历史批次列表、异常隔离、取消异常、恢复为当前数据、删除批次。
- 高风险批次操作：
  - “恢复为当前数据”弹出确认。
  - “删除批次”弹出确认。
- 工程审查修正：
  - `QualityAuditIssueRow` 新增原始 `Count`。
  - `RealtimeQualityCategoryRow` 新增原始 `Count`。
  - `AuditViewModel` 指标汇总使用原始数值，不从格式化字符串反解析。
- 导出约束仍保持：
  - 只保留 `数据管理 -> 导出当前筛选 Excel`。
  - 原生端未恢复 TXT、Markdown、一键三报表、`dump-public`、`dump-aircons`、旧报表页。
- 已验证：
  - `dotnet build native\EmsScout.Native.slnx -c Debug --no-restore /p:UseSharedCompilation=false`：0 警告，0 错误。
  - `dotnet test native\tests\EmsScout.Tests\EmsScout.Tests.csproj -c Debug --no-restore /p:UseSharedCompilation=false`：50/50 通过。
  - `npm run self-test` 通过。
  - `dotnet format native\EmsScout.Native.slnx --verify-no-changes --no-restore` 通过，仍有既有工作区加载警告。
  - `scripts\run-native.ps1 -NoBuild` 可拉起原生进程，窗口标题 `EMS 空调控制台`。
- 当前仍未完成的总目标事项：
  - 关注设备功能已有保存/筛选/导出字段，但当前 DB `device_watch_rules=0`，还需要做端到端样本验证。
  - 数据管理仍需继续做用户可用性打磨。

## 2026-07-06 原生 WinUI 分组设置可编辑化与启动口径修正

- 本轮继续推进“完整功能的原生 Windows 高级面板”，仍保持 5 个顶级页面：
  - 总览
  - 采集任务
  - 数据管理
  - 分组设置
  - 系统设置
- 分组设置从只读展示改为可编辑维护：
  - 注册 `IAreaGroupRepository` → `SqliteAreaGroupRepository`，修复点击分组页的运行期 DI 缺口。
  - `AreasPage.xaml` 改为真实维护界面：区域组列表、分组详情、命中统计、保存/删除、自定义成员添加/删除、跳转数据管理。
  - 系统组只读；自定义组可维护楼层、子区、设备成员。
  - 删除了“只展示已落地分组 / 自定义标签、备注和成员维护不作为当前进度展示”的误导文案。
- 自定义分组已接入数据管理和 Excel 导出：
  - `DeviceQuery.MonitorGroupIds` 进入 SQLite 查询。
  - 分组跳转数据管理后，`导出当前筛选 Excel` 使用同一筛选口径。
  - Excel 的 `summary` 和 `filters` sheet 写入 `filter_monitor_group_ids` / `MonitorGroupIds`，导出文件可追溯分组筛选来源。
- 分组仓储加固：
  - 关闭 reader 后再计算统计，避免同连接并发 reader 查询。
  - SQLite 聚合值用 `Convert.ToInt32` 读取，兼容 Int64 聚合结果。
  - 修复系统非公区统计 SQL 拼接的 `WHERENOT` 问题。
- 新增测试：
  - `AreaGroupRepositoryTests.cs`：覆盖系统组锁定、自定义组保存/删除、楼层/子区/设备成员、统计。
  - `CustomGroupExportTests.cs`：覆盖 `MonitorGroupIds` 查询过滤和 Excel 导出溯源。
- 原生应用启动口径修正：
  - 直接运行 `bin\...\EmsScout.Desktop.exe` 会因 Windows App SDK Runtime 没有包身份而触发 `REGDB_E_CLASSNOTREG`，不再作为验证方式。
  - 新增 `scripts/run-native.ps1` 和 `npm run native:run`，通过 `dotnet run`/MSIX package profile 启动。
  - `launchSettings.json` 移除 Unpackaged profile，避免误点裸 exe 路径。
  - 当前原生应用已用包身份打开，`EmsScout.Desktop` 进程响应中。
- 已验证：
  - `dotnet build native\EmsScout.Native.slnx -c Debug --no-restore /p:UseSharedCompilation=false`：0 警告，0 错误。
  - `dotnet test native\tests\EmsScout.Tests\EmsScout.Tests.csproj -c Debug --no-restore /p:UseSharedCompilation=false`：43/43 通过。
  - `dotnet format native\EmsScout.Native.slnx --verify-no-changes --no-restore` 通过。
  - `npm run self-test` 通过。
  - `node --check src\enumerate.js`、`node --check src\panel\server.js` 通过。
  - `package.json` JSON 解析通过。
  - 精确扫描未发现原生端接回 `scripts/report.js`、`dump-public`、`dump-aircons`、TXT/Markdown、多报表导出入口；`quality-report.js` 仅作为采集后的数据质量检查保留。

## 2026-06-29 右上角数据视图选择器与批次删除

- 本轮只在 `D:\Code\ems-tool-audit-fix` 操作，未触碰 `D:\Code\ems-tool` 主目录。
- 右上角原生 `select` 已替换为自定义“数据视图”选择器：
  - 默认显示 `当前数据`，对应最新正常已导入批次。
  - 下拉分组显示：
    - 当前数据。
    - 历史正常批次。
    - 疑似异常批次（仅未隔离时显示）。
    - 采集文件状态。
  - 已隔离批次不会出现在右上角选择器中。
  - 采集文件状态只作为状态提示，不再混同为数据源。
- 当前右上角显示：
  - `当前数据`
  - `2026-06-29 17:28 全量 (6568张) · 6568张 · 问题 2`
- 已隔离 run 3：
  - `2026-06-28 16:36 全量 (8958张)`
  - 仍保留在质量审计批次管理中。
  - 不出现在右上角数据视图选择器。
- 质量审计“批次管理”新增删除批次功能：
  - 每个批次行新增 `删除` 按钮。
  - 删除前弹窗要求手动输入 `确认删除`。
  - 后端 `DELETE /api/runs/:id` 也强制校验 `{ confirm: "确认删除" }`。
  - 删除按依赖顺序清理：
    - `run_cards`
    - `run_pages`
    - `run_sub_areas`
    - `run_buildings`
    - `collection_runs`
  - 删除的是历史批次快照，不直接删除当前 `cards/pages/sub_areas/buildings`。
- 新增后端函数：
  - `deleteRun(db, runId)` in `src/panel/history.js`
- 已验证：
  - 错误确认文字调用 `DELETE /api/runs/3` 返回 400，未删除。
  - 临时创建 run 5，使用 `确认删除` 删除成功，随后确认不存在残留。
  - 真实批次仍为 run 1-4。
  - Edge Playwright 验证右上角不显示 8958 张已隔离批次，质量审计批次表显示删除按钮。
  - `node --check web\panel\app.js src\panel\server.js src\panel\history.js` 通过。
  - `npm run self-test` 通过。
  - 页面截图：`out/run-picker-review-after.png`。
  - 面板服务已重启在 `http://127.0.0.1:17777`。

## 2026-06-29 质量审计升级为问题处置台

- 本轮只在 `D:\Code\ems-tool-audit-fix` 操作，未触碰 `D:\Code\ems-tool` 主目录。
- 修复质量审计默认口径：
  - `/api/quality` 未指定 `run_id` 时，不再读取可能过期的 `out/quality_report.json`。
  - 默认读取最新未隔离 `collection_runs` 批次的质量报告。
  - 当前默认质量页指向 run 4：`2026-06-29 17:28 全量 (6568张)`。
- 质量审计页从“统计卡片”升级为“问题处置台”：
  - 顶部新增审计结论：
    - `数据可用`
    - `建议复核`
    - `阻止导入`
  - 当前 run 4 结论为 `建议复核`。
  - 显示结论说明和质量报告生成时间。
- 新增问题分组：
  - `P1 问题`
  - `P2 问题`
  - `信息项`
- 每类问题显示：
  - 中文问题名。
  - 严重度。
  - 数量。
  - 原始说明。
  - 处理建议。
- 新增问题明细样例表：
  - 严重度。
  - 问题。
  - 位置。
  - 对象。
  - 建议。
  - 当前最多展示 80 条样例。
- 当前 run 4 质量概况：
  - 设备总数：6568。
  - 问题类型：2。
  - P2：未知通讯 1、缺 indicator 1。
  - INFO：重复渲染页 1、统一完整页 5、Inline 子区 1。
- 已验证：
  - `node --check web\panel\app.js src\panel\server.js` 通过。
  - `npm run self-test` 通过。
  - `GET /api/quality` 默认返回 `run_id=4`，未知通讯 1，缺 indicator 1。
  - `GET /api/quality?run_id=2` 返回 run 2 的历史质量报告，批次切换口径正常。
  - Edge Playwright 页面核查通过，无 console error。
  - 页面截图：`out/quality-audit-console-check.png`。
  - 面板服务已重启在 `http://127.0.0.1:17777`。

## 2026-06-29 区域管理改为区域组 / 包含范围

- 本轮只在 `D:\Code\ems-tool-audit-fix` 操作，未触碰 `D:\Code\ems-tool` 主目录。
- 根据同类 BMS/CMMS 产品做法，把原“区域 / 区域成员”改为更清晰的“区域组 / 包含范围”：
  - `区域管理` 页主标题改为 `区域组`。
  - `区域名` 改为 `区域组名`。
  - `区域标签` 改为 `用途标签`。
  - `区域成员` 改为 `包含范围`。
  - `目标区域` 改为 `加入到`。
  - `类型` 改为 `范围类型`。
  - `楼层 / 子区 / 设备` 改为 `整层 / 子区 / 单台设备`。
  - 页面顶部新增说明：先建区域组，再把整层、子区或单台设备加入它；公区/非公区是系统区域，不需要手动维护。
- 添加范围从手工输入改为选择式：
  - 楼栋：下拉选择。
  - 楼层：从楼层目录和已采集楼层中选择。
  - 子区：根据当前楼栋+楼层自动加载下拉。
  - 设备：根据当前楼栋+楼层+子区自动加载下拉。
  - 选择范围后顶部显示预览，例如 `整层范围：19 台设备`、`子区范围：19 台设备，开机 0`、`单台设备：104-KT-1 · 关机`。
- 新增后端接口：
  - `GET /api/area-options?building=...&floor=...&run_id=...`
  - 返回 `sub_areas` 和 `devices`，供区域组包含范围下拉选择使用。
- 数据管理页区域筛选按钮文案同步：
  - 多选具体分组时显示 `区域组 N`。
- 已验证：
  - `node --check web\panel\app.js src\panel\server.js` 通过。
  - `npm run self-test` 通过。
  - `/api/area-options?building=1号&floor=1F` 返回 1 个子区、19 台设备。
  - Edge Playwright 页面核查通过，无 console error。
  - 切换到 `1号 / 1F` 后子区显示 `1F · 19台`，设备列表显示 `104-KT-1` 等 1F 设备。
  - 通过接口临时创建 `临时验证区域组`，分别添加整层/子区/单台设备 3 个范围成功，随后删除临时组；当前未留下测试区域组。
  - 面板服务已重启在 `http://127.0.0.1:17777`。

## 2026-06-29 质量审计批次管理与默认采集导入

- 本轮只在 `D:\Code\ems-tool-audit-fix` 操作，未触碰 `D:\Code\ems-tool` 主目录；当前目录不是 git 仓库。
- 面板服务已重启在 `http://127.0.0.1:17777`，当前服务进程为 `node src/panel/server.js`。
- 采集任务默认任务已改为 `采集校验并导入`：
  - 后端任务链：`src/enumerate.js` → `scripts/validate-enum.js` → `scripts/import.js` → `scripts/quality-report.js --run-id=latest-run`。
  - 校验不通过会阻止导入；导入后自动审计最新导入批次，并写回 `collection_runs.quality_summary`。
  - `仅采集并校验` 仍保留，适合需要人工确认后再导入的场景。
- 采集方式不再固定只用 CDP：
  - 默认高级设置 `自动启动 Edge`，后端传 `--auto-launch`。
  - 可切换 `连接已有 Edge CDP`，后端传 `--edge`。
  - `src/enumerate.js` 已确认支持两种模式；`--auto-launch` 使用独立持久化 Edge profile。
- 质量审计页新增“批次管理”：
  - 批次表显示批次、实际/基线卡数、状态、质量、说明、操作。
  - 状态包括 `正常`、`疑似异常`、`已隔离`。
  - 操作包括 `隔离` / `取消隔离`、`恢复当前`、`审计`。
  - `恢复当前` 会从历史 run 表恢复当前 `buildings/sub_areas/pages/cards`，操作前有确认框。
- 历史表 schema 已新增并迁移：
  - `collection_runs.is_anomaly INTEGER NOT NULL DEFAULT 0`。
  - `quality_summary` 旧库迁移兜底。
  - `restoreCurrentFromRun` 兼容旧库的 `updated_at/raw_count/unique_count/duplicate_names/indicator/sub_idx` 字段。
- `/api/runs` 现在返回：
  - `expected_card_count`
  - `card_delta`
  - `suggested_anomaly`
  - `anomaly_reason`
  - `quality_issue_count`
  - 解析后的 `quality_summary`
- 当前批次状态：
  - run 4：`2026-06-29 17:28 全量 (6568张)`，正常，质量问题 2 项。
  - run 3：`2026-06-28 16:36 全量 (8958张)`，疑似异常，卡片数偏离基线 `+2390`，未手动隔离。
  - run 2 / run 1：均为 6568 张，质量问题 2 项。
- 已验证：
  - `node --check web\panel\app.js src\panel\server.js src\panel\history.js scripts\quality-report.js` 通过。
  - `npm run self-test` 通过。
  - `node scripts\quality-report.js --run-id=latest-run` 生成 `out/quality_report_run4.json` / `.txt` 并写回 run 4。
  - `/api/runs` 正常返回新字段。
  - Edge Playwright 核查通过：任务页默认 `collectImport`、采集方式默认 `autoLaunch`、6 栋默认全选；质量审计批次表正常渲染。
  - 隔离/取消隔离接口验证通过；取消隔离会清理默认 `采集数据异常，已隔离` 备注。

## 2026-06-28 全流程异常采集定位与防护

- 用户追问“点击全流程后会发生什么，为什么最近一次采集多采集了很多数据”。
- 已确认面板“全流程”原任务链为：采集 `src/enumerate.js --edge` → 导入 `scripts/import.js` → 质量审计 `scripts/quality-report.js` → 报表 `scripts/report.js`。
- 最近异常任务日志：`out/panel_task_20260628_162121.log`。
  - 原命令实际为 `node src/enumerate.js --edge --append --log-file`。
  - 导入生成 `History run: 3`。
  - 质量审计却跑了 `quality-report.js --run-id=1`，即用旧选中批次做审计。
- 异常根因表现：
  - 当前 `out/enum_full_v5.json` 里 1-6 号楼均为 30 子区、86 页、1493 张，首卡均为 `DXBCGQ-1`。
  - 当前 DB/run 3 每栋统计完全相同：1493 张、229 开机、1153 关机、111 离线。
  - 历史 run 1/run 2 均为 6568 张，分布正常。
  - 这不是新增真实设备，而是切楼后采集结果串页/重复读取 1 号楼形态并被导入。
- 已修复：
  - 新增 `src/enum-validator.js`，按楼栋基准、子区数、卡片数、首卡、采集签名检测异常。
  - 新增 `scripts/validate-enum.js`，面板全流程现在采集后、导入前先校验。
  - `scripts/import.js` 内置同样校验，绕过面板直接导入异常 JSON 也会停止。
  - `scripts/self-test.js` 使用人工小样本，测试环境显式设置 `EMS_SKIP_ENUM_VALIDATION=1` 跳过真实楼栋基准校验；正常面板和手动导入不设置该变量。
  - `src/enumerate.js` 保存前执行校验；异常采集不覆盖 `out/enum_full_v5.json`，改存 `out/enum_rejected_*.json`。
  - `src/panel/server.js` 全流程改为：采集 → 采集结果校验 → 导入数据库 → 审计最新数据 → 生成最新报表。
  - 全流程不再沿用当前下拉框选中的旧 `run_id` 做审计/报表。
  - 全量全流程不再传 `--append`；追加只在用户主动勾选且非全量全流程时生效。
  - `web/panel/index.html` 追加选项默认关闭，文案改为“追加/保留未采楼栋”。
  - `src/enumerate.js` 中 `--append` 语义补齐：非追加采集首次保存从空 JSON 开始，追加采集才保留旧楼栋。
- 已验证：
  - `node --check src\enum-validator.js scripts\validate-enum.js scripts\import.js src\panel\server.js src\enumerate.js web\panel\app.js` 通过。
  - `npm run self-test` 通过。
  - `node scripts\validate-enum.js` 对当前异常 JSON 返回失败，明确指出 2号/5号大幅超基准和 2-6 号楼签名相同。
  - 同一校验规则对历史 run 2 正常全量数据返回 `ok=true`。
  - 通过面板启动一次“仅导入”验证，异常 JSON 被拦截在导入前，`collection_runs` 未新增记录。
  - 面板服务已重启在 `http://127.0.0.1:17777`。

## 2026-06-28 取消全流程并优化采集任务

- 已按用户要求取消“全流程”。
- 顶部“全流程”按钮已移除；任务下拉不再提供 `pipeline`。
- 后端 `buildTaskSteps` 已删除 `pipeline` 类型；直接 POST `kind=pipeline` 会返回 unknown task。
- 新增任务类型：
  - `collectSafe`：采集并校验，只写采集 JSON，不导入数据库。
  - `enumerate`：仅采集到 JSON。
  - `validate`：仅校验当前采集结果。
  - `import`：仅导入数据库，导入脚本仍会先校验 JSON。
  - `quality`：仅质量审计。
  - `report`：仅生成报表。
- 采集任务页新增提示：采集任务不会自动导入数据库，需校验通过后手动导入。
- 全量 `collectSafe` 强制不传 `--append`；选定楼栋时可主动勾选“追加/保留未采楼栋”。
- 前端会根据任务类型禁用无关采集参数，降低误操作概率。

## 2026-06-29 数据管理区域筛选下拉化

- 数据管理页区域筛选从平铺复选框改成自定义下拉多选。
- 新增虚拟默认项 `全部区域`：
  - 默认选中。
  - 不写入数据库。
  - 表示不传 `groups` 参数，覆盖所有楼栋/楼层/设备。
  - 与 `公区`、`非公区`、自定义区域互斥。
- 具体区域仍支持多选，并沿用原有并集筛选逻辑。
- 选中区域会在下拉按钮旁显示彩色标签；下拉内选中项变色并显示勾选。
- 已验证：
  - 默认 `全部区域` 显示 8958 条。
  - 选择 `公区` 后显示 1206 条。
  - 点击 `全部区域` 后恢复 8958 条。
  - `公区 + 非公区` 多选并集显示 8958 条。
  - `node --check web\panel\app.js src\panel\server.js` 通过。
  - `npm run self-test` 通过。

## 2026-06-29 数据管理导出改为 XLSX 保存对话框

- 数据管理导出按钮文案改为 `导出 XLSX`。
- `POST /api/cards/export` 现在默认只生成一个 `.xlsx`，不再同时生成 `.txt`。
- 新增 `GET /api/files/download?path=...`，只允许下载 `out` 目录内文件，返回 Excel MIME 和附件文件名。
- 前端导出流程：
  - 先按当前筛选调用导出接口生成 XLSX。
  - 再下载为 Blob。
  - 优先使用 `window.showSaveFilePicker` 弹出浏览器保存位置选择。
  - 浏览器不支持时降级为普通下载。
- 已验证：
  - 公区筛选导出返回 1206 条，只生成一个 XLSX。
  - 下载接口返回 `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`，文件头 `PK` 正常。
  - Playwright 模拟 `showSaveFilePicker` 路径通过，保存文件名以 `.xlsx` 结尾且字节数 > 0。
  - `node --check src\panel\server.js web\panel\app.js` 通过。
  - `npm run self-test` 通过。

## 2026-06-29 数据管理筛选增强

- 数据管理页新增模式筛选 `filterMode` 和风速筛选 `filterFan`。
- 筛选栏文案调整：
  - `全部状态` → `全部开关`
  - 新增 `全部模式`
  - 新增 `全部风速`
- 后端 `loadCards` 新增查询参数：
  - `mode=制冷/通风/制热/...`
  - `fan=自动/高/中/低/0/...`
  - `mode=__unknown` / `fan=__unknown` 匹配空值或 `-`
- 新增 `GET /api/filter-options`，按当前批次动态返回实际存在的模式/风速及数量。
- 前端动态渲染模式/风速选项：
  - 模式：制冷 8004、通风 804、制热 138、未知 12。
  - 风速：自动 2670、高 3180、中 924、低 1818、默认0 354、未知 12。
- 导出 XLSX 已同步带上 `mode`、`fan` 筛选条件。
- 筛选栏增加 `compact-filters` 紧凑样式，桌面宽度下控件更紧凑，高度稳定。
- 已验证：
  - `/api/cards?mode=通风` 返回 804。
  - `/api/cards?fan=高&status=on` 返回 540。
  - `/api/cards?mode=__unknown&fan=__unknown` 返回 12。
  - Playwright 验证页面下拉选项、模式筛选、风速+开关组合筛选通过。
  - `node --check src\panel\server.js web\panel\app.js` 通过。
  - `npm run self-test` 通过。

## 2026-06-29 区域筛选默认态收敛

- 用户反馈数据管理页 `区域：全部区域` 突兀。
- 已调整：
  - 默认态按钮文案从 `区域：全部区域` 改为 `全部区域`。
  - 默认态不再额外显示 `全部区域` 彩色标签，避免重复。
  - 选中具体区域后显示 `区域 1` / `区域 N`，旁边显示具体区域彩色标签。
  - 区域按钮默认样式更接近普通 select，减少视觉突兀。
- 已验证：
  - 默认：label=`全部区域`，tags 为空。
  - 选择公区：label=`区域 1`，tags=`公区`，筛选 1206 条。
  - `node --check web\panel\app.js src\panel\server.js` 通过。
  - `npm run self-test` 通过。

## 2026-06-28 区域管理收敛

- 本轮仍只在 `D:\Code\ems-tool-audit-fix` 操作，未触碰 `D:\Code\ems-tool` 主目录。
- 面板对外术语从“重点监控/监控组”收敛为“区域管理/区域/区域成员”。
- 系统默认区域保留两个：`公区`、`非公区`，为锁定系统区域，按现有公区规则动态匹配。
- 自定义区域与 `公区`、`非公区` 并列，可添加楼层、子区、设备作为区域成员；楼层仍来自楼层目录/已采集楼层下拉选择。
- 数据管理页区域筛选改为复选框多选，多个区域按并集合并结果；导出当前筛选时按全量筛选结果导出，不再受列表页 1000 条显示上限影响。
- 面板任务中移除旧“刷新重点监控/重点监控报表”步骤；旧脚本和旧 `/api/monitor/*` 兼容接口暂保留，不再作为主 UI 功能暴露。
- 新增语义接口：`GET/POST/DELETE /api/areas`、`POST/DELETE /api/area-items`。
- 空的旧迁移残留 `未开放楼层` 区域会自动清理；若未来已有成员则按自定义区域保留。
- 已验证：
  - `node --check src\panel\monitor.js src\panel\server.js web\panel\app.js`
  - `npm run self-test`
  - `GET /api/areas` 只返回默认 `公区`、`非公区`
  - 公区筛选 1206，非公区筛选 7752，合并 8958
  - 区域导出总数与筛选总数一致
  - 自定义区域创建、添加楼层成员、筛选、删除流程通过
  - 面板服务已重启在 `http://127.0.0.1:17777`

## 2026-06-28 数据管理深度核实

- 用 API 和 Edge/Playwright 对数据管理页做端到端核实。
- 发现并修复：
  - `loadCards` 的默认 `_maxLimit` 被错误压成 1，导致数据管理页只显示 1 行；已恢复列表默认最多显示 500 行，导出仍可导出 20000 上限内的全量筛选结果。
  - “全部楼栋”时楼层下拉按楼栋重复显示同名楼层；已改为按楼层去重并显示 `楼层 · 全部楼栋`。
  - 报表列表会展示旧 `未开放楼层重点监控_*` 文件名；已在 `/api/reports` 过滤旧文件名，仅隐藏不删除真实文件。
  - 区域筛选复选框样式收紧，避免在筛选工具栏中过宽。
- 已覆盖验证：
  - 初始数据管理：8958 条，显示 500 行。
  - 楼栋筛选：1号 1493 条，显示 500 行。
  - 状态筛选：开机 1374 条，显示 500 行。
  - 区域筛选：公区 1206，非公区 7752，合并 8958。
  - 历史批次 run 2：公区列表 776，与区域统计一致。
  - 搜索 `DXBCGQ-1`：36 条，显示 36 行。
  - 标签增删、备注写入恢复、组合筛选导出均通过。
  - 导出 XLSX 表头正确，公区导出 1206 数据行。
  - 页面无 `console.error` / `pageerror`，不再出现旧“重点监控/监控组”文案。

## 2026-06-28 区域管理深度核实

- 用 API 和 Edge/Playwright 对区域管理页做端到端核实。
- 发现并修复：
  - 系统区域删除接口原先返回 500；已改为可预期的 400，错误为“系统区域不能删除”。
  - 系统区域原先可通过成员接口手动添加成员；已在后端拒绝，并在前端只允许自定义区域进入“目标区域”下拉。
  - 无自定义区域时，“加入区域”按钮禁用，目标区域提示“请先新建自定义区域”。
  - 设备类型成员原先缺少设备名输入框；已新增 `设备名` 输入，并按成员类型启用/禁用 `子区`、`设备名` 字段。
  - 区域操作增加错误提示，避免静默失败。
- 已覆盖验证：
  - 默认区域只有 `公区`、`非公区`，无旧文案。
  - 系统区域不能删除，系统区域不能手动添加成员。
  - 自定义区域创建、编辑、添加楼层成员、数据管理按区域筛选、删除成员、删除区域通过。
  - 子区成员筛选 19 条，设备成员筛选 1 条。
  - UI 设备成员流程通过：设备名 `DXBCGQ-1` 可添加并在数据管理筛出 1 条。
  - 无测试区域/测试成员残留，系统区域手动成员数为 0。
  - `node --check src\panel\monitor.js src\panel\server.js web\panel\app.js` 和 `npm run self-test` 通过。

## 2026-06-28 总览深度核实

- 用 API 和 Edge/Playwright 对总览页做端到端核实。
- 发现当前“最新数据”run 3 / current DB 本身异常：总数 8958，且 1-6 号楼都显示 1493 张、229 开机、1153 关机、111 离线；2-6 号楼明显偏离基线并与 1 号楼统计完全重复。
- 质量报告仍是 6568 口径，和当前 8958 数据不一致；后端 `/api/summary` 已补 `quality.stale`、`quality.expected_total`、`quality.report_total`。
- 总览页已新增：
  - 数据异常告警区。
  - 状态分布图：开机/关机/离线。
  - 区域分布图：公区/非公区。
  - 楼栋开机率横向柱图。
  - 楼栋异常高亮：重复统计且大幅偏离基线时标记“疑似异常”。
- 当前最新数据会显示：
  - `质量口径不一致`
  - `当前数据 8958 张，质量报告 6568 张`
  - `楼栋数据疑似异常：2号、3号、4号、5号、6号`
- 切换到 run 2（2026-06-28 15:34 全量 6568 张）后：
  - 告警消失。
  - 质量显示 `质量 2 项问题`。
  - 楼栋数据恢复正常分布。
- 已验证无 `console.error` / `pageerror`，无旧“重点监控/监控组”文案，`npm run self-test` 通过。

## 当前阶段

报表 P0、P1、P2 已完成并验证通过。第三阶段“升级数据模型”暂时归档，未执行。
本轮已完成 P2 后全流程复跑、导入、质量报告、报表验证，以及串行真实页面对比。
本轮进一步把等待策略收敛到“页面可见即继续”的方向，避免通用质量门拖满 45 秒。

## 工作目录

- 本轮只在 `D:\Code\ems-tool-audit-fix` 操作。
- 未修改 `D:\Code\ems-tool` 主目录。
- 当前目录不是 git 仓库。

## 报表 P0 优化

- `scripts/report.js`
  - 设备名改为自然排序。
  - MD 首页新增报表摘要、楼栋汇总、数据质量说明、口径说明。
  - TXT 头部新增状态汇总、楼栋汇总、重复渲染页集中说明、口径说明。
  - XLSX 新增 `报表说明` sheet。
  - XLSX 明细和汇总写入筛选、列宽。
- `scripts/verify-reports.js`
  - 校验 `报表说明` sheet。
  - 校验 XLSX 筛选、列宽、重复页说明。

## 报表 P1 优化

- `scripts/report.js`
  - 新增开机风险评分：公区开机、低设定温度、高风速、开机但离线、重复渲染等因素累加。
  - MD/TXT/XLSX 摘要新增“开机风险 Top / 楼层开机 Top / 座区统计 / 异常标签说明”。
  - 未关闭空调 XLSX 明细新增 `风险等级`、`风险分`、`风险原因` 三列。
  - 未关闭空调 XLSX 在公区/非公区分组内按风险优先排序。
  - 异常标签统一补齐：通讯未知、开关未知、缺 indicator、通讯离线、开机但离线、模式未知、温度异常。
- `scripts/verify-reports.js`
  - 校验 P1 摘要段存在。
  - 校验状态类 XLSX 明细风险列存在。
  - 保留原有统计口径、筛选、列宽、重复渲染标注校验。

## 报表 P2 优化

- `scripts/report.js`
  - XLSX 新增 `风险明细` sheet，含风险等级、风险分、风险原因、设备定位、通讯、模式、温度、风速和备注。
  - XLSX 新增 `异常明细` sheet，含异常标签、设备定位、开关/通讯、风险等级/风险分和重复渲染备注。
  - MD/TXT/XLSX 摘要新增风险分布和异常分布。
  - `风险明细` / `异常明细` 均写入筛选和列宽。
- `scripts/verify-reports.js`
  - 校验 `风险明细` / `异常明细` sheet 存在。
  - 校验关键列、筛选、列宽。
  - 校验 `报表说明` 包含风险分布和异常分布。

## 全流程复跑与实际页面核对

- 复跑链路：`node src/enumerate.js --edge --log-file` → `node scripts/import.js` →
  `node scripts/quality-report.js` → `node scripts/report.js --type=all --type=on --type=off --format=md --format=xlsx --format=txt --out=out/report-full-p2`。
- 结果：`6568 cards, 142 sub-areas across 6 buildings`。
- `npm run verify:reports -- --dir=out/report-full-p2` 通过。
- `npm run self-test` 通过。
- 实际页面串行核对通过：
  - `4号 1F`
  - `1号 13F --page=三页`
  - `2号 2.5F`
  - `6号 7F --page=一页`
- 现象说明：多页楼层未指定 `--page` 时，`verify-live.js` 会按整层 DB 口径展示 `dbNotLive`，不代表漏采。
- 本轮新增：`adaptivePolling` / `qualityCheckWithProgressiveRetry` 接受稳定部分页，避免已渲染页面继续空等。

## 报表 P2 验证

- 新报表路径：`out/report-p2`，共 9 个文件。
- `node scripts/report.js --type=all --type=on --type=off --format=md --format=xlsx --format=txt --out=out/report-p2`：通过。
- `npm run verify:reports -- --dir=out/report-p2`：通过，DB total=6568，ON=1878，notON=4690，duplicate rendered pages=1。
- `npm run self-test`：通过。
- `node --check scripts/report.js scripts/verify-reports.js`：通过。
- 抽样确认：未关闭 XLSX 包含 `汇总 | 报表说明 | 风险明细 | 异常明细 | 1号...6号`；`风险明细` 1042 行，`异常明细` 7 行。

## 当前数据状态

- `out/ac.db` 总卡数：6568。
- 未关闭设备：2016。
- 未开启设备：4552。
- 唯一重复渲染页：2号 2.5F default，`raw=10 unique=7`。
- 重复设备：`2-2BC-GQ-KT-1`、`2-2BC-GQ-KT-2`、`2-2BC-GQ-KT-3` 各 2 份渲染。
- 质量报告路径：`out/quality_report.txt`、`out/quality_report.json`。

## 当前限制

- 当前仅依赖 `xlsx`。该库能稳定写入筛选和列宽，但不会写出 Excel 冻结窗格 XML；冻结窗格未纳入本轮交付。
- P0/P1/P2 均只改报表层展示和验证，不改变采集逻辑、SQLite 结构和统计口径。

## 面板采集任务与最新采集时间优化（2026-06-29）

- 右上角数据批次默认项从固定“最新数据”改为动态“最新采集：YYYY-MM-DD HH:mm (N张)”。
- `/api/runs` 返回结构调整为 `{ runs, latest_collection }`，其中 `latest_collection` 读取 `out/enum_full_v5.json` 的 `completedAt`、卡片数、楼栋数，并在采集结果新于最新导入批次时标记 `pending_import`。
- 总览“运行分布”徽标同步显示默认最新采集时间；手动选择历史批次时仍显示历史批次 label。
- 采集任务楼栋默认全选 1号-6号，新增“全选”“清空”和“已选 N 栋”计数。
- 采集/校验/导入类任务在未选择任何楼栋时前端拦截，避免空选择被后端解释为全量。
- 任务参数页保留常用设置：楼栋、任务类型、开始/停止；日志级别、日志类别、补采、追加、日志文件、自检、关闭网络监听移入“高级设置”折叠区。
- 验证：`node --check web\panel\app.js src\panel\server.js` 通过；`npm run self-test` 通过；Edge Playwright 页面核查通过，截图：`out/task-settings-latest-check.png`。

## 总览异常批次保护（2026-06-29）

- 核查发现默认总览展示当前最新数据库批次 `2026-06-28 16:36 全量 (8958张)`，该批次 2-6 号楼统计与 1 号楼重复且明显偏离楼栋基线。
- 前端新增楼栋基线卡数表和批次健康判定：全量批次卡数与基线总数偏差超过 2% 时判为疑似异常。
- 总览首次打开/刷新时，如最新采集批次疑似异常且用户未手动选择批次，自动切换到最近正常历史批次；当前数据下自动选中 `2026-06-28 15:34 全量 (6568张)`。
- 自动切换时总览显示 info 提示，说明最新采集批次异常并已切到最近正常批次。
- 用户仍可在右上角手动选择“最新采集”，此时总览展示 8958 张异常数据，并显示质量口径不一致和楼栋疑似异常两个告警。
- 验证：默认总览 KPI=6568、下拉选中 run 2；手动切回最新采集 KPI=8958，告警 class 为 `alert warn` + `alert danger`；`node --check` 和 `npm run self-test` 通过。
## 2026-07-07 数据管理页 UI 紧凑化与导出路径复核

- 按第一性原则和对抗性审查结果继续收敛原生数据管理页：
  - 筛选卡最大高度收紧到 280，基础筛选改为左对齐固定宽度控件组，减少宽屏留白。
  - 表格继续保留 `MinWidth=1120` 与水平滚动，避免窄窗口挤压列。
  - 结果列表/详情比例保持 `2.6* / 0.8*`，详情不再压缩主表格。
  - 数据管理页移除最近 Excel 列表首屏入口，仅保留“导出当前筛选 Excel”和“打开上次导出”。
- 导出语义复核：
  - `DataViewModel` 导出完成文案改为“上次导出：...”，错误文案改为“无法打开上次导出位置”。
  - 当前数据管理页主导出仍只有“导出当前筛选 Excel”。
  - 历史批次导出仍被 ViewModel 和 `SqliteDeviceExportService` 阻止。
  - legacy `scripts/report.js` / `dump-aircons.js` / `dump-public.js` 仍默认禁用，不接入原生 UI。
- 运行态截图证据：
  - 宽屏最终图：`out/ui-data-page-final-compact.png`。
  - 1280 宽窗口图：`out/ui-data-page-final-compact-1280.png`。
- 验证：
  - `dotnet build native\EmsScout.Native.slnx -c Debug --no-restore /p:UseSharedCompilation=false` 通过，0 警告 0 错误。
  - `dotnet test native\tests\EmsScout.Tests\EmsScout.Tests.csproj -c Debug --no-restore /p:UseSharedCompilation=false` 通过，61/61。
  - 源码扫描确认原生主 UI 未恢复 TXT、Markdown、一键三报表、旧报表页；命中的旧脚本均为 legacy/诊断说明或质量审计链路。
