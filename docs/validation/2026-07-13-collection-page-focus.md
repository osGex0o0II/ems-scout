# 采集页聚焦本机验证

日期：2026-07-13

## 范围

- 使用 `scripts/run-native.ps1 -NoBuild -UiValidation` 启动 Debug AppX。
- 设置、数据和导出目录位于唯一临时目录：`%TEMP%\ems-scout-ui-validation-f3a5ef372db64fb78bab16e5a1b3c409`。
- 本记录是隔离本机 UI 验证，不是真实 EMS 现场 E2E；未启动采集任务，未访问生产数据库。

## 自动化证据

- `npm run native:test`：236/236 通过，0 失败，0 跳过。
- `npm run native:build`：0 warning，0 error。
- UI 合同覆盖：检查面板不折叠、通过进度、状态画刷、进入动画和重排动画。

## 运行时观察

- 1280x752 窗口中，本地采集组件、当前数据、采集浏览器、EMS 页面四项始终可见。
- 检查结果为 `2/4 已通过` 时，进度条显示 50%；圆形底和状态文字保持主题灰色，通过/阻塞仅用低饱和灰绿/灰棕图标区分。
- 顶部摘要仍明确显示 `运行前检查未通过` 和首个卡点 `卡在采集浏览器：请先打开采集浏览器`。
- 状态区、采集范围、高级设置和右侧任务进度无文字或控件重叠。
- 重新检查会重建状态行，触发错峰进入和重排动画；没有持续闪烁效果。
