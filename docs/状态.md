# 当前状态

更新时间：2026-07-18 +08:00

## 结论

产品重构的跨平台逻辑阶段已完成 21/21 项。C#/.NET 10 是产品与数据库主干，
Node 仅保留 EMS 浏览器采集，PowerShell 负责 Windows 打包和现场验证。

Windows CI 工作流已扩展到 WinUI/XAML 编译、干净克隆测试、Sidecar payload smoke、
临时测试证书签名和干净用户 MSIX 安装生命周期。本地 Release 签名构建已验证；远端首次
闭环运行待当前分支推送后确认。正式生产签名、版本升级、内置 Sidecar 实采和真实 EMS
尚未在目标 Windows 环境验收。

## 当前数据基线

权威黄金清单：`tests/fixtures/run17/golden-v1.json`

| 指标 | 数量 |
|---|---:|
| 楼栋 | 6 |
| 子区 | 143 |
| 页面 | 373 |
| 原始卡片 | 6571 |
| 唯一卡片 | 6568 |
| 开机 | 1843 |
| 关机 | 3196 |
| 离线 | 1527 |
| 未知通讯 | 2 |

楼栋唯一卡片：1号 1493、2号 107、3号 1106、4号 1096、5号 286、6号 2480。

原生数据管理包含 2 个虚拟纳管设备，因此无筛选 Excel 为 6570 行数据加 1 行标题。

## 已完成重构

1. 楼层模型支持小数楼层。
2. 启动迁移恢复和错误 UI。
3. 采集期间设置锁与全局排他 lease。
4. schema guard 和迁移层唯一 DDL 所有权。
5. SQLite 导入事务、取消和故障回滚。
6. legacy 清单、删除门和安全清理。
7. `enumerate.js` 的纯逻辑模块拆分。
8. SQLite 基础设施统一。
9. Node/C# 质量规则共用 fixture。
10. 数据库破坏性、并发、幂等和黄金一致性测试。
11. Excel 内容级测试和生产 DB 测试隔离。
12. `CollectionTaskViewModel` 环境探测、进度解析和对账行模型拆分。
13. 统一错误类别、错误码和安全用户文案。
14. 原生 NDJSON 结构化日志、脱敏、截断和诊断页接入。
15. 当前架构、状态和交接文档收口。
16. 产品显示名统一为 `EMS Scout`，C# 技术标识统一为 `EmsScout`。

## 当前验证

- 本地非生产证据 .NET：197/197。
- Node/Sidecar/契约/架构/枚举/field/security：119 项，117 通过、2 个缺生产 fixture 的明确跳过。
- Debug MSIX 已刷新生成（约 100.4 MiB、418 entries），包内 DB/WAL/SHM/现场证据命中数为 0；尚未执行实际安装。
- `npm run self-test`：通过。
- Windows 原生整套 build：0 warning / 0 error，BuildTools 与目标平台均为 26100。
- `dotnet format`：退出成功。
- 临时 DB ExportSmoke：`2.5F` 7 行；`xlsx` 读取为 `devices`、12 列、`A1:L8`。
- 生产 JSON、质量报告、DB、WAL、SHM 哈希未变化。
- CI 迁移门禁改用合成 v0 fixture；上传清单和 legacy Electron 包均排除 DB/WAL/SHM、枚举、质量和实时生产证据。

## 当前生产文件哈希

```text
out/enum_full_v5.json
80c5f175b043387beb36fb64e4b0b573c919141ed9a32ef416af174e1d205716

out/quality_report_run17.json
3c164fa0d0c9aaf3ffbb9e8eb55a4b20c9ee686eeb3543f5879ddcb3641ea2f3

out/ac.db
7bf26085ac5d8d0259c29bdcfdaaa72ce6cc4f31bacb478e20f5c72118350cda
```

## Git 审计

- `git diff --check` 通过，未发现密钥或异常大文件。
- 10 个删除项均有替代实现，运行时引用已清空。
- 生产 `out/*` 没有 Git 内容变更。
- `data/1号楼`、`data/2号楼` 的 4 个未跟踪 WAL/SHM 明确排除，不删除、不暂存。
- 重构已按四组提交：`b362d30`、`f0b8fcb`、`aeb6739`、`5d59c52`。
- 本状态同步另行提交，文档不固定记录自身提交哈希。
- 当前分支为 `main`；提交后的工作树只允许保留上述 4 个受保护未跟踪文件。

## 外部未完成

- 正式生产证书签名与发布验证。
- Windows x64 MSIX 跨版本升级验证。
- 安装包内置 Sidecar 实际采集验证（payload smoke 已通过）。
- 真实 EMS 单栋和六栋全量现场验证。
- 外部门通过后的 protected legacy 删除。
